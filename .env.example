# Copie para .env e ajuste. O .env nunca é commitado.

# API — conexão com o Postgres (usada a partir da fatia #173).
DATABASE_URL=postgresql+psycopg://traveltogether:traveltogether@localhost:5432/traveltogether

# Web — endpoint da API.
TRAVELTOGETHER_API_URL=http://localhost:8000
